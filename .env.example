# PASS — environment

# Gemini (the interrogator + question author). Optional: with no key the game
# runs fully on the offline bank + heuristic scorer, so the judge can always
# play. Get a key at https://aistudio.google.com/apikey
# Accepts any of: GEMINI_API_KEY / GOOGLE_API_KEY / GOOGLE_GENERATIVE_AI_API_KEY
GEMINI_API_KEY=

# Neon (daily seed + leaderboard + run transcripts) — stretch
# DATABASE_URL=

# Algolia (Hall of the Passed transcript search) — stretch
# NEXT_PUBLIC_ALGOLIA_APP_ID=
# ALGOLIA_ADMIN_KEY=
# NEXT_PUBLIC_ALGOLIA_SEARCH_KEY=
